services:
  - type: web
    name: karavan-booking-bot
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: BB_SECRET_KEY
        generateValue: true
      - key: BB_AUTH_USER
        value: admin
      - key: BB_AUTH_PASS
        generateValue: true
