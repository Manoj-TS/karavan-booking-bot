# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5

# Data / ORM
sqlmodel==0.0.22
pyyaml==6.0.2
openpyxl==3.1.5

# Portal HTTP + parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Captcha (OCR optional; manual entry always works)
pillow==11.1.0
pytesseract==0.3.13

# AI trekker parsing (optional; falls back to local heuristic if key absent)
anthropic==0.69.0
